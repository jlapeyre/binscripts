#!/bin/sh

# Set up two keyboard layouts: 'us' (standard US) and 'us-intl' (US International with dead keys).
# Pressing both Shift keys simultaneously toggles between them.
# This allows entering accented characters when needed, without changing keyboard hardware.

setxkbmap -layout "us,us" -variant ",intl" -option "grp:shifts_toggle"
