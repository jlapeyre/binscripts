#!/bin/sh

# Two screens
# Two external monitors ON
# Laptop screen OFF

xrandr --output DisplayPort-1 --auto --left-of HDMI-A-0 \
       --output HDMI-A-0 --auto --primary \
       --output eDP --off
